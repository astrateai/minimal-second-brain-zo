#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VAULT_DIR="$(cd "${SCRIPT_DIR}/../.." && pwd)"
python3 "${VAULT_DIR}/.ops/scripts/health_check.py" --vault "${VAULT_DIR}"
